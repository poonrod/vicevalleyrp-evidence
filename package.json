{
  "name": "vicevalleyrp-evidence",
  "private": true,
  "workspaces": [
    "api",
    "web",
    "shared"
  ],
  "scripts": {
    "dev:api": "npm run dev -w api",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w shared && npm run build -w api && npm run build -w web",
    "db:migrate": "npm run db:migrate -w api",
    "db:seed": "npm run db:seed -w api",
    "db:generate": "npm run db:generate -w api",
    "hostinger:build:api": "npm run build -w shared && npm run db:generate -w api && npm run build -w api && node scripts/sync-api-dist.js",
    "hostinger:start:api": "node dist/server.js",
    "hostinger:build:web": "npm run build -w shared && npm run build -w web",
    "hostinger:start:web": "npm run start -w web"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}
