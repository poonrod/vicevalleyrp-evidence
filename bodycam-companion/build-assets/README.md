# Build assets

- **`app-icon.png`** — Vice Valley logo used to build the Windows **`.exe` icon** (`icon.ico` is generated when you run `npm run pack:win`).
- Replace `app-icon.png` if branding changes, then repack.

`icon.ico` is generated next to it during packaging and is gitignored if listed in the parent `.gitignore`.
